// Test stub for SvelteKit's `$env/dynamic/public`, which only exists under the
// SvelteKit build. Aliased in vitest.config.ts so component tests can import
// modules that read public env without pulling in the full SvelteKit runtime.
export const env: Record<string, string> = {};
